# 강지애 프로필 프로젝트 지침

## 프로젝트 개요
강지애 프로필 페이지를 관리하는 프로젝트.
최신 이력을 업데이트할 수 있는 구조를 유지하며, 기본 디자인 스타일을 반드시 지켜야 한다.
다른 스타일로의 변경은 사용자가 요청할 때만 진행한다.

---

## ⚠️ 필수 규칙 1 — 수정 파일 항상 명시
작업이 끝나면 **반드시** 수정한 파일 목록을 응답 마지막에 명시한다.

예시:
> **수정 파일:** `style.css`, `index.html`

파일이 하나여도, 여러 개여도 예외 없이 표기한다.
사용자가 GitHub 등에 직접 업로드해야 하기 때문에 누락하면 안 된다.

---

## ⚠️ 필수 규칙 2 — 기존 코드 최소 수정
기존 파일을 수정할 때는 **변경이 필요한 부분만 최소한으로 수정**한다.

- 전체 파일을 다시 쓰지 않는다. `Edit` 도구로 해당 부분만 교체한다.
- 기능 추가 시: 기존 로직은 건드리지 않고, 새 코드를 덧붙이는 방식을 우선한다.
- 기능 제거 시: 실제로 사용되는 부분(HTML·CSS·JS)만 정확히 삭제한다.
- 이 원칙은 디자인·스타일 변경 요청에도 동일하게 적용한다.

---

## ⚠️ 필수 규칙 3 — 모던 웹 가이드라인 준수
웹페이지를 생성하거나 수정할 때는 아래 모던 웹 표준을 반드시 지킨다.

### 반응형 (Responsive)
- 모든 페이지는 기본적으로 **반응형**으로 제작한다.
- `<meta name="viewport" content="width=device-width, initial-scale=1" />` 필수 포함.
- 모바일(≤640px), 태블릿(≤980px), 데스크탑 3단계 브레이크포인트 적용.
- 좌우 여백: 모바일 20px, 태블릿 24px, 데스크탑 40px 이상 확보.
- `padding` 단축형이 다른 클래스의 좌우 패딩을 덮어쓰지 않도록 주의 (`padding-left`/`padding-right` 분리 사용 권장).
- `overflow-x: hidden` 으로 가로 스크롤 방지.

### 접근성 (Accessibility)
- 이미지에 `alt` 속성 필수.
- 버튼/링크에 `aria-label` 또는 명확한 텍스트 제공.
- 색상 대비 WCAG AA 기준 이상 유지.
- 키보드 탐색 가능하도록 `focus` 스타일 유지.

### 성능 (Performance)
- 외부 폰트·라이브러리는 CDN 사용, `preconnect` 힌트 포함.
- 이미지는 `object-fit: cover` + 적절한 크기 지정.
- 불필요한 인라인 스타일 최소화, CSS 클래스로 관리.
- **이미지 최적화**: 새 이미지 추가 시 WebP 변환 권장. `<picture>` 태그로 WebP + PNG 폴백 구조 사용.
  ```html
  <picture>
    <source srcset="assets/image.webp" type="image/webp">
    <img src="assets/image.png" alt="..." class="...">
  </picture>
  ```
- **모든 공개 페이지**에 `<meta name="theme-color" content="#f4ede2">` 포함 (모바일 주소창 색상 통일).

### 시맨틱 HTML
- `<header>`, `<nav>`, `<main>`, `<section>`, `<footer>`, `<article>` 등 시맨틱 태그 사용.
- 제목 계층 `h1 → h2 → h3` 순서 준수.
- 링크에 `href`, 폼에 `label` 연결 필수.

### CSS 품질
- CSS 변수(`--variable`) 활용으로 색상·간격 일관성 유지.
- 기존 `style.css`의 디자인 토큰(색상, 폰트, 간격) 그대로 사용.
- 미디어 쿼리는 모바일 퍼스트 또는 데스크탑 퍼스트 중 프로젝트 방향 통일.

---

## 디자인 스타일 토큰 (변경 금지)
```
--bg: #f4ede2          /* 따뜻한 베이지 배경 */
--paper: #fbf6ec       /* 카드 배경 */
--ink: #1b1a18         /* 주 텍스트 */
--accent: #2a3b2e      /* 포인트 딥 그린 */
--accent-warm: #b3753a /* 웜 카퍼 */
--gold: #c9a35b        /* 골드 */

폰트: Pretendard (본문) / Noto Serif KR (제목·포인트) / JetBrains Mono (코드·날짜)
```

---

## ⚠️ 필수 규칙 4 — 푸터에 "Made with Claude" 표기
모든 신규 페이지의 `<footer>` 하단 `.foot-bottom` 영역에 반드시 아래 문구를 포함한다.

```html
<span class="mono">Made with Claude · v 1.0 · {페이지명}</span>
```

- 기존 페이지(index.html, all.html, edu.html, high.html)에는 이미 적용되어 있다.
- 신규 페이지 생성 시 예외 없이 동일하게 적용한다.

---

## ⚠️ 필수 규칙 5 — 작업 완료 전 최종 검수
작업을 마치기 **전에** 반드시 스스로 최종 검수를 수행하고, 결과를 응답 마지막에 명시한다.

### 검수 체크리스트
- [ ] HTML 태그 열고 닫힘 오류 없음
- [ ] JS 함수 호출 및 변수 참조 오류 없음
- [ ] 새로 추가한 함수/변수명 충돌 없음 (기존 코드와 중복 없음)
- [ ] 기존 기능(저장·수정·삭제·달력·목록)이 그대로 동작하는지 점검
- [ ] 디자인 토큰(색상 변수) 올바르게 사용했는지 확인
- [ ] 반응형: 모바일 미디어쿼리 영향 없음
- [ ] Apps Script 수정 시: 기존 doGet/doPost 라우팅 로직 보존 여부 확인

검수 완료 후 이상 없음을 확인한 뒤에만 작업 완료를 알린다.
검수 중 문제가 발견되면 사용자에게 알리기 전에 먼저 수정한다.

### 검수 결과 표기 형식 (응답 마지막에 반드시 포함)
```
✅ 검수 완료
- HTML 태그: 이상 없음
- JS 로직: 이상 없음
- 기존 기능: 보존 확인
- 디자인 토큰: 준수
```

---

## ⚠️ 필수 규칙 6 — 작업 완료 시 수정 파일 명시 (규칙 1 보강)
작업이 완료되면 응답 마지막에 수정된 파일을 반드시 명시한다.

- 규칙 1과 동일하나, **GitHub 업로드 대상 파일**임을 명확히 인식시킨다.
- 파일이 여러 개인 경우 각각 나열한다.
- 사용자가 직접 업로드해야 하므로 누락 시 작업이 반영되지 않는다.

예시:
> **수정 파일 (GitHub 업로드 필요):** `schedule.html`, `schedule-apps-script.txt`

---

## 사이트 구성도 (서브 페이지 포함)

```
강지애 프로필 사이트
│
├── index.html            # 메인 프로필 페이지 (공개)
│   ├── #about            소개 섹션
│   ├── #competency       핵심역량 섹션
│   ├── #certificates     자격·연수 섹션
│   └── #experience       강의 경력 섹션 (구글 시트 API 연동)
│
├── all.html              # 수업활용앱 모음 페이지 (페이지 공개 · 앱 추가/수정만 PIN)
│   ├── 구글 시트에서 앱 목록 로드 (카테고리: 퀴즈/시뮬레이션/게임/도구/수업)
│   └── 앱 추가·수정 (PIN 인증 후)
│
├── edu.html              # 실시간 교육 모니터링 페이지 (공개)
│   └── 구글 시트 공고목록 연동 + 카카오 알림
│
├── schedule.html         # 강의 스케쥴 관리 페이지 (PIN 보호 · 비공개)
│   ├── 달력 뷰 (월별 강의 일정)
│   ├── 강의 목록 뷰 (이달 강의 테이블)
│   ├── 강의 추가/수정/삭제
│   ├── 자격 · 연수 등록/수정/삭제 (자격증현황 시트)
│   ├── PDF 다운로드
│   └── 경력등록 완료 버튼 (지난 강의 → 경력사항 자동 입력)
│
└── high.html             # 고교학점제 진로설계 실습 앱 (수업용 · 독립 페이지)
```

---

## 파일 구조
```
profile/
├── index.html                  # 메인 프로필 페이지
├── all.html                    # 수업활용앱 모음 페이지
├── edu.html                    # 실시간 교육 모니터링 페이지
├── schedule.html               # 강의 스케쥴 관리 페이지 (PIN 보호)
├── high.html                   # 고교학점제 진로설계 실습 앱 (수업용)
├── style.css                   # 공유 스타일시트 (index + edu 공통)
├── stats.json                  # 통계 데이터
├── resume-download.js          # 경력서 PDF 생성 스크립트 (jsPDF + html2canvas)
├── resume-apps-script.txt      # 경력서용 구글 시트 Apps Script (참고용)
├── schedule-apps-script.txt    # 강의 스케쥴용 구글 시트 Apps Script (참고용)
├── apps-gallery-script.txt     # 수업앱 목록용 구글 시트 Apps Script (참고용)
└── assets/
    ├── portrait.png        # 프로필 사진
    ├── daisy.png           # 데이지 로고 (네비게이션 · PIN화면 공통)
    ├── flower-bar.png      # 하단 플라워 바 이미지 (PNG 폴백용)
    └── flower-bar.webp     # 하단 플라워 바 이미지 (WebP 최적화, 107KB)
```

---

## 페이지별 기술 명세

### index.html — 메인 프로필 페이지
- **공개 여부**: 공개
- **외부 의존**: `style.css`, `resume-download.js`, `stats.json`
- **stats.json**: 통계 카운터 데이터. 카드에 `source` 필드가 있으면 시트 연동(아래). **카드 순서: 출강개소 → 자격종수 → IT경력 → 연수시간** (370시간 카드가 마지막).
  - 기본값은 **시트 연동이 실패했을 때 보이는 값**이다. 실제 수치보다 낮게 두면 그대로 낮게 노출되므로 가끔 맞춰준다. 2026-09-09 기준 실제 출강 34개소 → 기본값 20 → **30** 으로 상향.
- **캐시**: 경력·자격 데이터를 `localStorage` `kja_profile_v1` 에 저장해 **먼저 그리고 뒤에서 갱신**한다. 공개 페이지라 방문자마다 Apps Script 를 기다리면 칩·카드가 1~3초 늦게 나타났다. 칩/카드 추가 로직이 "이미 있으면 건너뛴다" 방식이라 캐시본·최신본을 연달아 그려도 중복되지 않는다.
- **주요 버튼**: 명함 다운로드 (html2canvas 캡처), 경력서 다운로드 (resume-download.js 호출)
- **시트 자동 연동 (페이지 하단 인라인 스크립트, `RESUME_API_URL` 전역 재사용)**:
  - **강의 경력 학교 칩**: `.schools[data-org-prefix]` (에듀인플랫폼 / 제주대학교 찾아가는) — 시트 경력의 `주요업무`에서 학교명을 추출해 **기존 칩은 유지하고 새 학교만 중복 없이 추가** (학교 접미사 정규화로 중복 판정).
  - **통계 숫자**: `.num[data-source]` (schools/certs)에 **5단위 내림** 표시 (예: 23→20+). schools=학교 칩 distinct 수, certs=시트 자격증 수. stats.json 카드의 `source` 필드로 연결, `.numval`/`.plus`/`.unit` 구조.
    - ⚠️ 통계 그리드는 `stats.json` 을 받아 그리므로 시트 연동 스크립트와 **순서가 엇갈린다.** 예전에는 150ms×15회 재시도 후 포기해 값이 20+ 에 머무는 일이 있었다. 이제 **양쪽이 서로를 부른다** — 시트 값이 오면 `applyStatNumbers`, 그리드가 그려지면 `window.__applyStatCounts()`. 나중에 준비된 쪽이 반영한다.
    - 실측(2026-09-09): 출강 34개소 → 30+, 자격증 13종 → 10+.
  - **자격·연수 카드**: 시트 `certificates`를 자격 섹션 카드로 **새 항목만 중복 없이 추가**. 분류는 `group`(edtech/national) → 비었으면 발급기관 자동분류, `연수/교육`→연수 탭. 급수(`extra`)·이수시간 표시.

### resume-download.js — 경력서 PDF 생성 스크립트
- **RESUME_API_URL**: `https://script.google.com/macros/s/AKfycbzu3aLNJNMPlwmf1648mua6sED-94nHXEIdpJXoQl7mfFtyYxmMu9EJVjTrnZ2ine6nhA/exec`
- ⚠️ **schedule.html의 SCHEDULE_API_URL과 완전히 동일한 URL** — 같은 Apps Script 웹앱 사용
- 연결 시트: `학력사항`, `경력사항`, `자격증현황`
- PDF 출력 순서: 학력사항 → 경력사항(+에듀인/찾아가는 그룹) → 자격증 → 연수 및 교육
- **경력 가공 (`processCareer`)**:
  - **통합 계약 (`MERGE_CONTRACTS`)**: 대정중학교(2026.03.09~2026.12.31·디지털튜터)·남원중학교(2026.04.01~2026.11.30·강사)는 여러 강의를 **한 줄로 통합**하고 비고에 "현재 진행중" 표기. `start`/`end`/`work`는 코드 상단에서 한 줄로 수정. 정렬은 종료일 기준 → 진행중 계약이 상단.
  - **별도 그룹 (`GROUP_BLOCKS`)**: `에듀인플랫폼`, `제주대학교 찾아가는`(학교컨설팅)은 경력사항 하단에 소제목 달고 따로 묶고 **비고를 비움**.
  - **정렬**: 경력 최신순(내림차순) + **완전중복 행 제거**.
- **PDF 렌더링 (재설계)**: 각 페이지를 **고정 A4 크기**로 렌더(297mm 캡 제거 → 잘림 방지) + margin 없는 padding 기반 높이 측정 + 안전여백. 가로줄 테이블, 페이지마다 푸터+페이지번호, 페이지 넘김 시 섹션 제목·컬럼헤더 반복(제목/행이 경계에서 잘리지 않음).

### all.html — 수업활용앱 모음
- **공개 여부**: **페이지는 공개**(누구나 앱 목록 열람). 단 **"앱 추가"·각 카드 "수정" 버튼은 PIN 인증**으로 보호.
- **PIN 게이트**: `_allPin` 모듈 (XOR 인코딩 + SHA-256 + 5회 실패 잠금, SALT `kja-all-2026`, **schedule.html과 동일 PIN**). 세션: `sessionStorage.getItem('kja_all_v1') === '1'`. 인증 성공 시에만 추가/수정 모달이 열림. (예전의 평문 `PASSWORD`/`allAuth` 방식은 폐기됨)
- **APPS_API_URL**: `https://script.google.com/macros/s/AKfycbw0vDZEh7uVvHFq8Y_H60VIoyi6exVjkMYZ9PSMHI3foTLlol9PLyl7BroQPDx8i6lKfA/exec`
- **Apps Script 파일**: `apps-gallery-script.txt` (schedule/edu와 **별개 배포**)
- **연결 시트**: `앱목록` (컬럼: 앱이름A | 설명B | URL C | 카테고리D | 등록일E | **ID F**)
- **카테고리**: 퀴즈 / 시뮬레이션 / 게임 / 도구 / **수업** (필터 버튼 + 추가/수정 폼 옵션, `catClass` 매핑 `cat-class`)
- **앱 추가/수정**: 추가 모달을 추가·수정 공용으로 사용. 수정은 카드 "수정" 버튼 → PIN → 제목·설명·URL·카테고리 변경 → `action:'update'`로 ID(F열) 기준 갱신(등록일 유지).
- URL이 비어 있으면 `DEFAULT_APPS` 하드코딩 폴백 (폴백 항목은 ID가 없어 수정 불가)
- ⚠️ **기존 시트 항목 수정 불가 문제**: `앱목록` 시트 F열(ID)이 비어 있으면 수정 버튼 클릭 시 "시트에 저장된 항목이 아닙니다" 오류. Apps Script(doGet)가 F열 자동 백필하므로 **스크립트 재배포 후 페이지 로드 시 자동 해결**. 또는 시트 F열에 수동으로 고유 ID 입력해도 됨.
- ⚠️ **apps-gallery-script.txt 재배포 필요 시점**: 구버전 스크립트는 F열(ID)을 반환하지 않아 수정 기능이 작동 안 함. 반드시 최신 로컬 파일 내용으로 재배포할 것.

### edu.html — 실시간 교육 모니터링
- **공개 여부**: 공개 (PIN 없음)
- **APPS_SCRIPT_URL**: `https://script.google.com/macros/s/AKfycbxZsgsakUwZJES61mpJNK7amyIFI7IYOpxwrHgAGeD0LkYqQ3Tb3ak1DLO-8ygF3no/exec`
- ⚠️ **schedule/resume URL과 다른 별도 Apps Script 배포**
- **연결 시트**: `수집기관` (GET `?sheet=수집기관`), `공고목록` (GET `?sheet=공고목록`)
- **카카오 알림**: `data.sheet === '알림요청'` POST 액션으로 처리 (스크립트 내장)
- **로컬 캐시**: `localStorage` 사용 (수집기관 목록, 공고 데이터 보존)
- 기관 추가/공고 수동 등록 기능 포함

### schedule.html — 강의 스케쥴 관리
- **공개 여부**: PIN 보호 (XOR 인코딩 + SHA-256 + 5회 실패 시 잠금)
- **SCHEDULE_API_URL**: `https://script.google.com/macros/s/AKfycbzu3aLNJNMPlwmf1648mua6sED-94nHXEIdpJXoQl7mfFtyYxmMu9EJVjTrnZ2ine6nhA/exec`
- ⚠️ **resume-download.js의 RESUME_API_URL과 완전히 동일** — 하나의 Apps Script로 통합 관리
- **Apps Script 파일**: `schedule-apps-script.txt` (resume + schedule + 카카오 통합)
- **연결 시트**: `강의스케쥴` (14컬럼), `경력사항` (경력등록), **`자격증현황` (자격·연수 관리)**

#### ⚠️ 강의스케쥴 시트 컬럼 지도 (새 컬럼 추가 전 반드시 확인)
컬럼 번호가 파일마다 흩어져 있어 **실제로 충돌이 날 뻔했다**(참여형태를 M에 넣으려다 캘린더ID를 덮어쓸 뻔함).

> 2026-09-09 실제 시트를 열어 눈으로 확인한 값이다. **새 컬럼을 만들기 전에 반드시 시트를 다시 열어볼 것** — 코드만 보고 판단하면 틀린다(아래 경고 참조).

| 열 | 이름 | 쓰는 곳 | 상수 |
|---|---|---|---|
| A~J (1~10) | ID·날짜·구분·업체명·수강방식·출강학교단체·시간·시수·수업주제·비고 | `handleSchedule_` add/update 가 `getRange(row,1,1,11)` 로 한 번에 씀 | — |
| K (11) | 경력등록 | `handleSchedule_` (`complete`) | 하드코딩 `11` |
| L (12) | 정산등록 | `notion.gs` | `COL_SETTLED` |
| M (13) | 캘린더ID | `calendar.gs` | `CAL_EVENTID_COL` |
| N (14) | 동기화서명 | `calendar.gs` (**배포본에만 있음**) | — |
| O (15) | 참여형태 | `Code.gs` | `COL_ROLE` |
| P (16) | **예상금액** | `Code.gs` (입력) · `notion.gs` (노션 '예정' 행) | `COL_EXPECTED` |

> 🚨 **`schedule-calendar-script.txt` 는 배포본보다 오래됐다.** 저장소 백업에는 `CAL_EVENTID_COL`(M) 만 있고 **N열 `동기화서명` 로직이 없다.** 배포된 `calendar.gs` 가 더 최신이므로 이 백업 파일로 덮어쓰면 안 된다. 참여형태를 N에 넣으려다 동기화서명을 날릴 뻔했다(2026-09-09, 배포 직전에 발견).

- K 이후 컬럼은 `appendRow`/`setValues` 범위(1~11) 밖이라 **개별 `setValue` 로 따로 기록**한다. 덕분에 수정 시 서로의 값을 덮어쓰지 않는다.
- **참여형태**: `강사` / `보조강사` / `안전요원` (디싹 안전요원 업무 반영). 빈 값이면 `강사`로 본다.
- 노션 `참여형태`에는 안전요원 옵션이 없어 정산 시 **`안전요원` → `운영지원`** 으로 보낸다(과거 "어린이날 행사 스태프"도 운영지원으로 기록됨). 옵션 오염 방지 원칙 유지.
- **시간 형식**: `HH:MM-HH:MM` 또는 `HH:MM~HH:MM` 둘 다 허용 (정규식 `/[~\-]/`)
- **회사 색상**: `PRESET_COMPANIES` 객체에 하드코딩 + `AUTO_PALETTE` 자동 배정, `localStorage`에 저장
- **달력 셀 클릭**: 날짜 칸의 빈 영역 클릭 시 해당 날짜로 강의 추가 모달이 열림(하루 2건 이상 등록 가능). 뱃지 클릭은 수정.
- **자격 · 연수 관리(신규)**: "자격 · 연수" 섹션에서 자격증/연수를 **분리 폼**으로 등록·수정·삭제. `loadCerts()`가 base GET(`certificates`)으로 로드, 카테고리로 자격증/연수 테이블 분리. 액션 `certAdd`/`certUpdate`/`certDelete`. 등록 데이터는 인덱스 자격 섹션·경력서 PDF에 자동 반영.

#### 강의 추가/수정 모달 (2026-09-09 개선)
같은 업체 강의가 하루에 여러 건일 때 **엉뚱한 강의를 열어 덮어쓰는 사고**가 있었다. 원인은 뱃지가 업체명만 보여줘 구분이 안 되고, 3건째부터는 아예 열 수 없었던 것.

- **달력 뱃지에 시작 시각 표시** — `10:00 에듀인` 형태(`.ev-tm`). 시간이 없으면 업체명만. 같은 업체 여러 건이 구분된다.
- **`+N건 더보기` 클릭** → 데이 시트가 열려 그날 일정 전체에 접근 가능. 데스크톱에서는 가운데 패널로 뜬다(`@media(min-width:641px)`). 예전에는 클릭이 칸으로 흘러가 **추가 모달**이 열렸고 3건째 강의는 열 수 없었다.
- **수정 모드 배너**(`#editBanner`) — "기존 일정을 **수정** 중입니다 — 09.02 에듀인플랫폼". 저장 버튼도 `변경 저장`으로 바뀐다.
- **추가 모드 안내**(`#dayHint`) — 고른 날짜에 이미 있는 일정을 나열해 중복·착각을 막는다. 날짜를 바꾸면 갱신된다(`onDateChange`).

#### 달력 뱃지 — 업체명 전부 표시 (2026-09-09)
업체명이 `남원중학…` 처럼 잘려 보이던 문제. **두 줄 구조**로 바꿔 이름이 칸 너비를 혼자 쓰게 했다.
```
● 에듀인플랫      ← .ev-top (점 + .ev-txt, word-break:break-all 로 줄바꿈)
  폼
  18:00           ← .ev-tm (아랫줄, 작은 모노)
```
- `.ev-txt` 는 자르지 않는다(`trunc` 제거) — 칸이 좁으면 줄바꿈으로 감싼다. `.cal-cell` 이 `min-height:90px` 라 셀이 알아서 늘어난다.
- 칸 80px(달력 `min-width:560px` 최악 조건)에서 검증: 업체명 10종 **잘림 0건**, 가로 스크롤 없음. 5자 이하 1줄 / 6자 이상 2줄.
- 모바일(≤640px)은 뱃지 자체가 숨겨지므로 영향 없음.

#### 회차 반복 등록 (2026-09-09)
같은 강의를 여러 날에 나눠 할 때 한 번 입력으로 N건을 만든다. **추가할 때만** 보이고 수정 모드에서는 숨긴다(`showRepBox`).

- **매주 같은 요일**: 일시 칸 날짜부터 7일 간격 × N회(2~20). 월·연·윤년을 넘어도 요일이 유지된다.
- **날짜 직접 선택**: 날짜를 칩으로 쌓는다. 첫 회차(일시 칸 날짜)는 뺄 수 없다.
- **회차 표시**: 수업주제에 `(1/4)` … `(4/4)` 자동 부여 (끌 수 있음).
- 저장은 기존 `add` 액션을 **N번 순차 호출**한다 — Apps Script·시트 스키마 변경 없음. 진행률이 버튼에 뜨고, 일부 실패 시 실패한 날짜만 알려준다.
- 정산 모달에 **전체 선택 / 전체 해제**(`settleAll`)를 넣어, 회차 N건이 한 번에 입금될 때 클릭 한 번으로 묶을 수 있다.

#### 경력 미등록 감시 (2026-09-09)
경력등록을 안 하면 **프로필의 출강 학교 칩·통계에 반영되지 않는다.** 스케쥴을 아무리 잘 넣어도 프로필이 비어 보인다.
- `pendingCompletesAll()` — 기간 제한 없이 지난 강의 중 `경력등록 ≠ Y` 전부.
- 목록 헤더 밑 `#completeNote` 에 **보고 있는 달이 아닌** 미등록 건수와 해당 월을 표시한다. 달을 넘겨 잊은 건을 놓치지 않기 위한 것.
- 이달 미등록이 2건 이상이면 `경력등록 N건 일괄` 버튼(`completeMonth`)이 함께 뜬다.
- 도입 즉시 2026-07-16 프리랜서·제주문화예술센터 1건이 2개월째 누락돼 있던 것을 잡아냈다.

#### 예상금액 → 노션 '예정' 행 자동 생성 (2026-09-09)
강의를 추가·수정할 때 **예상금액을 넣으면** 노션에 `완료여부=예정 · 입금여부=입금대기` 행이 바로 생긴다. 노션 **월별 최종내몫 차트가 앞으로의 달까지 채워진다.**
- 시트 **P열 예상금액**(`COL_EXPECTED`)에 저장 → `notion.gs` `syncNotionPlan_()` 이 노션에 반영.
- **금액이 없으면 아무것도 하지 않는다.** 금액 없는 빈 행이 노션에 쌓이는 걸 피하기 위한 것. 빠르게 일정만 넣을 때는 예전과 동일하게 시트에만 들어간다.
- **정산이 끝난 행(L열='Y')은 건드리지 않는다** — 확정 금액을 예상금액으로 덮어쓰지 않기 위해.
- 나중에 정산하면 `handleNotionSettle_` 이 **같은 `일정ID` 행을 찾아 갱신**한다(중복 없음).
- 노션 호출이 실패해도 **강의 저장은 성공으로 둔다.** `syncNotionPlan_` 은 예외를 던지지 않고 로그만 남긴다.
- 주관업체는 `NOTION_COMPANY_MAP` 으로 추정하고, 없으면 비워 둔 뒤 비고에 `업체확인필요: OO` 를 남긴다(옵션 오염 방지). 정산할 때 모달에서 정확히 고르면 갱신된다.
- ⚠️ 이전에는 "정산 시점에만 노션 행을 만든다"가 원칙이었다(2026-09-09 오전). 예상금액이 들어가면서 **빈 행이 아니게 되어** 방침을 바꿨다.

#### 경력 미등록 — 월간 메일 알림 (2026-09-09)
**경력등록은 자동화하지 않는다.** 무엇을 경력으로 남길지는 사람이 보고 판단하는 게 맞다고 보아, 대신 **매월 1일 오전 9시**에 미등록 건을 메일로 알린다.
- `Code.gs` : `monthlyCareerReminder()` · `pendingCareerRows_()` · `careerParseDate_()` · `careerIsPast_()`
- 받는 사람 `CAREER_ALERT_TO` = `BASIC_INFO.email`. **카카오가 아니라 메일**을 쓴 이유 — 기존 카카오 알림은 `공고목록`(교육 공고 마감임박)용이고 `KAKAO_REST_KEY` 설정 상태도 불확실하다. `MailApp` 은 설정 없이 바로 동작한다.
- 미등록이 0건이면 **메일을 보내지 않는다**(조용한 달은 조용하게).
- `careerIsPast_` 는 페이지의 `isEventPast` 와 같은 규칙(종료시각 있으면 그 기준, 없으면 익일 0시).
- **설치**: 편집기에서 `setupCareerReminderTrigger` 를 한 번 실행. **테스트**: `monthlyCareerReminder` 직접 실행.

#### 저장 실패 시 재시도 (2026-09-09)
`apiPost` 가 실패하면 900ms 뒤 한 번 다시 보낸다. 단 **`RETRYABLE` 목록에 있는 액션만** — `update`·`delete`·`certUpdate`·`certDelete`·`syncCalendar`·`notionLedger`.
> ⚠️ `add`·`complete`·`certAdd` 는 **재시도하면 안 된다.** 요청이 서버에 닿은 뒤 응답만 실패해도 재시도하면 행이 두 번 들어간다. 이들을 안전하게 재시도하려면 클라이언트가 만든 요청ID를 시트에 기록해 중복을 거르는 구조가 필요하다(미구현).

#### 돈 관련 표시 — 노션 원장 읽기 (2026-09-09)
시트에는 금액이 없고 **노션이 원장**이므로, 페이지의 돈 표시는 전부 `action:'notionLedger'` 로 읽어온다.

- **서버**: `notion.gs` `handleNotionLedger_` — `기간 >= from`(기본 18개월 전) 전체를 페이징(최대 20페이지)해 가벼운 객체로 반환. `notionRowToObj_` 가 title/date/school/company/status/done/gross/net/payback/payDate/schedId 만 추린다.
- **캐시**: `localStorage` `kja_ledger_v1`, TTL 10분. 캐시본으로 먼저 그리고 뒤에서 갱신한다. 정산 등록 성공 시 `loadLedger(true)` 로 즉시 새로고침.
- **미수금 카드**: `기간 <= 오늘` + `입금여부 ∈ {입금대기·미정산·확인필요}`. **앞으로 할 강의는 제외**한다(아직 받을 돈이 아님). 클릭하면 `#unpaidOverlay` 목록. 30일 넘으면 빨갛게.
- **이달 입금 카드**: `입금여부=입금완료` 중 **입금일**이 그 달인 건. 입금일이 비어 있으면 강의일로 본다(정확도가 조금 떨어지니 노션에 입금일을 채우는 게 좋다).
- 시트에 없는 건(월급제 월 단위 행·과거 수기 입력분)까지 전부 잡히는 것이 이 방식의 이점이다.

#### 경력등록 규칙은 서버에 있다 (2026-09-09 이관)
`markComplete` 는 이제 `{action:'complete', id}` 만 보낸다. 근무기간·직위·주요업무 가공은 `Code.gs` 의 **`buildCareerRow_`** 가 한다(정산과 같은 방식 — 규칙 변경 시 Apps Script 한 곳만 고침).
- 하위 호환: `period`/`org`/`position`/`work` 가 모두 채워져 오면 그대로 쓴다. 캐시된 옛 페이지도 계속 동작한다.
- **묶음 경력등록**: 목록 헤더의 `경력등록 N건 일괄` 버튼(`completeMonth`) — 그달의 지난 강의 중 미등록 건이 2개 이상일 때만 나타난다. 회차 강의용.

#### 스케쥴 데이터 캐시
`localStorage` `kja_sched_data_v1` 에 마지막 조회분을 저장. 캐시로 먼저 렌더 후 시트에서 받아 다시 렌더하고, **조회 실패 시 캐시를 유지**한다(예전에는 `data=[]` 로 비웠다).

### high.html — 고교학점제 진로설계 실습 앱
- **공개 여부**: 공개 (수업용 독립 페이지)
- **외부 API 없음**: 모든 과목 데이터가 JS 내 하드코딩
- **PIN/비밀번호 없음**: 완전 공개
- **기능**: 진로 유형별 과목 추천, 이수 계획 시뮬레이션, PDF 출력

---

## ⚠️ Apps Script 프로젝트 지도 (2026-08-28 정리 — 반드시 먼저 읽을 것)

프로젝트가 여러 개이고 이름이 비슷해 **엉뚱한 곳을 고치는 사고가 실제로 있었다.** 아래 표가 유일한 기준이다.

| 프로젝트 이름 | 편집 링크 | 담당 | 시트 연결 |
|---|---|---|---|
| **강의스케쥴 API** | [1OKUmoVs…](https://script.google.com/d/1OKUmoVsNX-KqFD_JqsxPCL4_dEjK_KJhCleo2Phb7MaLH_a7-nQnlvVf/edit) | schedule.html · resume-download.js · edu.html · 노션 정산 연동 | **바인딩 아님** — `경력사항` 시트를 `SPREADSHEET_ID` 로 직접 연다 |
| 제목 없는 프로젝트 (앱갤러리) | [1qkNDhNn…](https://script.google.com/d/1qkNDhNnyvbMF0zVJY3rj0roLo6CgARw5ZB679o_sZZzFrZyEK84SAf-H/edit) | all.html (앱목록) | `경력사항` 스프레드시트에 바인딩 |

**규칙 1 — 스케쥴·경력서·교육공고·노션 정산 관련 수정은 `강의스케쥴 API` 프로젝트에서만 한다.**
파일 구성: `Code.gs`(본체) + `notion.gs`(노션 정산 연동). 저장소 백업본은 각각 `schedule-apps-script.txt`, `schedule-notion-script.txt`.

**규칙 2 — 앱갤러리 프로젝트는 절대 재배포하지 않는다.**
그 프로젝트의 배포는 옛 버전에 고정돼 있고, 현재 코드에는 스케쥴 로직이 없다(과거 `제목 없음.gs` 삭제됨). 재배포하면 고정이 풀려 API가 죽는다. 코드 열람은 무방.

**규칙 3 — 노션 토큰은 `강의스케쥴 API` 프로젝트의 스크립트 속성 `NOTION_TOKEN` 에만 둔다.**
공개 페이지인 `schedule.html` 의 JS에 토큰을 넣지 않는다. 스크립트 속성은 프로젝트별로 분리되므로 다른 프로젝트에는 두지 않는다.

### 웹앱 URL 정리

| 스크립트 | 변수명 | 쓰는 파일 |
|---|---|---|
| `schedule-apps-script.txt` (+ `schedule-notion-script.txt`) | `SCHEDULE_API_URL` / `RESUME_API_URL` | schedule.html · resume-download.js · edu.html **공용** |
| `apps-gallery-script.txt` | `APPS_API_URL` | all.html 전용 |
| (별도) | `FILE_API_URL` | all.html 파일 업로드 |

> ⚠️ 세 페이지가 같은 URL을 공유한다. URL이 바뀌면 `schedule.html` · `resume-download.js` · `edu.html` **세 곳을 모두** 고쳐야 한다.

---

## 경력 데이터 관리 방식

### 데이터 흐름
```
구글 스프레드시트 → Google Apps Script (웹앱) → index.html
```

- 경력 데이터는 구글 스프레드시트에서 관리한다.
- 스프레드시트에는 3개의 시트가 있다: `학력사항`, `경력사항`, `자격증현황`
- Google Apps Script 웹앱 URL이 `resume-download.js` 상단 `RESUME_API_URL` 변수에 설정되어 있다.

### 경력 업데이트 방법 (사용자 직접 관리)
- **일상적인 경력 추가**: 구글 스프레드시트에 직접 행을 추가한다.
- **여러 건 한꺼번에 정리**: Claude에게 내용을 주고 표 형식으로 정리 요청 후 복붙한다.
- 시트를 저장하면 다음 경력서 다운로드 시 자동 반영된다. 별도 배포 불필요.

### 다운로드 기능 (index.html 버튼)
- **명함 다운로드**: index.html 내 카드 디자인을 html2canvas로 캡처해 이미지로 저장
- **경력서 다운로드**: 구글 시트 데이터를 API로 가져와 jsPDF + html2canvas로 A4 PDF 생성
  - 학력사항 → 경력사항 → 자격증 → 연수 및 교육 순서로 출력
  - 경력사항은 학력사항 바로 아래에 이어서 시작 (행이 많아 페이지 넘김 자동 처리)
  - 테이블이 페이지를 넘어갈 때 섹션 제목과 컬럼 헤더가 다음 페이지 상단에 반복됨
  - 인쇄 친화적 디자인 (흰 배경, 연한 색상만 사용)

---

## 스케쥴 → 경력 자동 등록 기능

### 기능 개요
schedule.html 달력에서 **지난 날짜의 강의**에 "경력등록" 버튼이 나타나며, 클릭 시 구글 시트 `경력사항` 탭에 자동으로 행이 추가된다.

### 데이터 매핑 규칙 (스케쥴 → 경력사항)
| 스케쥴 필드 | 경력사항 필드 | 변환 방식 |
|---|---|---|
| `date` | `근무기간` | `2026-05-26` → `2026.05.26` |
| `company` | `기관/직장명` | 그대로 사용 |
| `type` | `직위/역할` | 교육수강→수강 / 디지털튜터→디지털튜터 / 나머지→강사 |
| `school`+`hours`+`topic` | `주요업무` | `{school} ({hours}h) — {topic}` 형식 조합 |
| (없음) | `퇴직/종료사유` | "완료" 고정 |

### 완료 상태 추적
- 구글 시트 `강의스케쥴` 탭의 11번째 컬럼 `경력등록` 에 `Y` 저장
- 달력에서 이미 등록된 강의는 "✓ 경력등록됨" 뱃지로 표시 (버튼 없음)

### 버튼 표시 조건
- 달력 뷰에서만 표시 (목록 테이블에는 없음)
- 이벤트 날짜 < 오늘 인 경우에만 표시
- `교육수강` 타입도 포함 (수강 이력으로 등록)

### Apps Script 액션 목록 (`schedule-apps-script.txt`, POST `action`)
| action | 기능 |
|---|---|
| `add` | 스케쥴 추가 |
| `update` | 스케쥴 수정 |
| `delete` | 스케쥴 삭제 |
| `complete` | 경력사항 등록 + 완료 플래그 저장 |
| `notionAdd` | 노션 정산 원장에 행 생성 + 정산등록 플래그 저장 (`notion.gs`, 구버전·유지) |
| `notionSettle` | **정산 등록** — 입금 확인 후 여러 강의를 금액까지 한 번에 기록 (`notion.gs`) |
| `notionLedger` | **정산 원장 읽기** — 미수금·월별 입금 카드용 (`notion.gs`) |
| `certAdd` | 자격증/연수 추가 (자격증현황 시트) |
| `certUpdate` | 자격증/연수 수정 (ID 기준) |
| `certDelete` | 자격증/연수 삭제 (ID 기준) |

> `apps-gallery-script.txt`(별도 배포)는 앱 추가(기본 POST) + `action:'update'`(앱 수정)을 지원하며, `앱목록` 시트의 ID 컬럼(F)을 사용한다.

---

## schedule.html 모바일 · 홈 화면 앱 (2026-08-29)

### 구글 캘린더 동기화 — calendar.gs v2.2 (2026-09-09)
저장소 백업 `schedule-calendar-script.txt` 를 **배포본(v2)에 안전장치 하나를 더한 v2.1 로 교체했다.** 이제 이 파일이 배포본과 일치하므로 통째로 붙여넣어도 된다(이전에는 v1이라 붙여넣으면 동기화서명 로직이 사라졌다).

**v2 가 이미 갖고 있던 것 (내가 착각했던 부분)**
- `if (lastRow < 2) return out;` — 빈 시트 보호가 이미 있었다. v1 백업에만 없었다.
- N열 `동기화서명`(`calSig_` DJB2 해시) 비교로 **바뀐 행만** 캘린더에 쓴다. v1은 매번 120건을 전부 다시 써서 할당량에 걸렸다.
- `CAL_MAX_OPS = 150` — 한 번 실행의 캘린더 쓰기 상한. 넘치면 `partial:true` 로 멈추고 다음 실행에서 이어간다.
- 태그 중복 이벤트 정리, v1 행 서명 자동 채택(쓰기 0회).

**v2.1 에서 추가한 것 — `keepCount` 가드**
행은 있는데 A열(ID)·B열(날짜)이 비어 유효한 일정을 하나도 못 잡으면, 뒤따르는 정리 루프가 `!keep[tag]` 판정을 전부 참으로 보고 **태그 붙은 660일치(과거 120 + 미래 540)를 통째로 삭제**한다. `keepCount === 0` 이면 삭제·중복정리를 건너뛰고 로그를 남긴다.

**v2.2 에서 추가한 것 — 알림을 하루 1회로**
예전에는 강의 1건마다 전날 18시 팝업이 떴다. 대정중처럼 주 4~5일 나가면 거의 매일, 하루 3건이면 팝업이 3개 울렸다. 이제 **그날 첫 강의 하나에만** 알림을 건다.
- 판정: `firstByDate` — 같은 날짜에서 가장 이른 시각, 동률이면 시트에서 위에 있는 행. `syncCalendarAll_`(2-pass)과 `syncCalendarOne_` 이 **같은 규칙**을 쓴다(다르면 두 경로가 알림을 붙였다 뗐다 한다).
- 요약 일정을 따로 만들지 않으므로 캘린더 화면은 그대로다.
- `calSig_(spec, mark)` 의 `mark` 는 **앞으로의 일정에만** `'R1'`/`'R0'` 를 넣는다. 지난 강의는 `''` 라서 v2 서명과 같아 건너뛴다 — 덕분에 v2.2 로 올릴 때 재작성 대상이 112건이 아니라 **미래 53건(약 3회 실행)** 으로 줄어든다.
- ⚠️ 실측: 앞으로의 강의 53건 중 알림은 43개(하루 2건 이상인 날이 6일뿐이라 10개만 줄어듦). 그중 30개가 월급제 정기 일정(대정중·남원중·백록초)이다. 더 줄이려면 `SALARY_COMPANIES` 를 알림에서 제외하면 13개(월 4개)가 된다 — 2026-09-09 시점에는 **적용하지 않기로** 결정.

**주의**
- 이 스크립트는 M(13)·N(14) 열만 쓴다. **O열 참여형태는 `Code.gs` 담당**이라 건드리지 않는다.
- `ensureCalHeader_` 가 `getMaxColumns() < 14` 일 때 컬럼을 늘리므로, 시트 컬럼을 함부로 지우면 안 된다.

### 홈 화면 앱 (PWA)
- `schedule.html` 만 아이폰 홈 화면에 앱으로 설치한다. 전용 아이콘 `assets/schedule-icon-180/192/512.png`(달력 모양, 사이트 팔레트), 설정은 `schedule.webmanifest` + head의 apple-mobile-web-app-* 메타.
- 사이트 공용 `assets/apple-touch-icon.png`(데이지)는 다른 페이지용이므로 건드리지 않는다.

### 모바일 달력 (<=640px)
- `.calendar { min-width:0 }` 로 7일이 가로 스크롤 없이 들어온다. 데스크톱은 기존 560px 유지.
- 셀 안의 뱃지·경력등록·정산 버튼은 숨기고 **색점(`.m-dots`)** 으로만 요약한다. 점의 색 = 업체 색, 경력등록 완료 건은 흐리게.
- 날짜를 탭하면 **데이 시트(`#daySheet`)** 가 아래에서 올라오고 거기서 수정·경력등록·정산·추가를 한다. 데스크톱 동작(칸 클릭 = 강의 추가)은 그대로.
- z-index: 데이 시트 400 < 수정 모달 오버레이 500. 모달이 시트 위에 뜬다.
- `render()` 끝에서 시트가 열려 있으면 `renderDaySheet()` 로 같이 갱신한다.

### PIN 저장 위치
`sessionStorage` → **`localStorage`** 로 변경(키 `kja_sch_v1`). 홈 화면 앱은 실행할 때마다 세션이 새로 시작돼 매번 PIN을 묻기 때문. 기기당 1회만 입력한다.

---

## 노션 정산 원장 연동 (2026-08-28)

### 원칙 (2026-09-09 개정 — 정산 모달 도입)
- **정산은 입금이 확인된 시점에 한 번만 기록한다.** 강의 직후에는 아무것도 만들지 않는다. 정산은 보통 강의 2주~2개월 뒤에 이뤄진다.
- 한 번의 입금에 **여러 강의가 묶여** 들어오므로, 정산 모달에서 같은 업체의 미정산 강의를 함께 골라 강의료를 나눠 넣는다. 노션에는 **강의별로 행 1개씩** 생성된다(기존 데이터 구조와 동일).
- 사용자가 입력하는 값은 **건별 강의료 · 계좌 입금액 · 세금 있음/없음** 뿐이다. 소득구분·세율은 묻지 않는다.
- **원천징수세액은 입력받지 않는다** — `강의료 − 실입금액` 으로 자동 역산해 기록한다.
- ~~`예상금액` 속성은 만들지 않는다~~ → **정정(2026-09-09)**: 실제로는 노션에서 `예상금액`을 적극 활용 중이다(미입금 건 대부분에 값이 있음). 미수금 카드는 이 값을 **1순위**로 쓴다 — 세전총액은 세금 전이라 과대하고, 예상금액이 실제 들어올 액수에 가장 가깝기 때문. 우선순위: `예상금액 → 세전총액 → 실입금액`, 예상치인 건은 목록에 회색 `예상` 표시.
- 미수금은 schedule.html **정산 대기 섹션**(월 구분 없이 전체, 경과일 표시)에서 확인한다.

### 세율 자동 판정 (`NOTION_TAX_RATE`, schedule.html)
과거 정산 기록에서 확인된 업체별 원천징수 세율. 사용자는 `세금 있음` 만 체크한다.
- **3.3%**: 에듀인플랫폼 · 에듀인플랫폼(한라도서관) · 제주평생교육진흥원 · 남원중 · 대정중 · 백록초 · 도민대학 · 목포대랄랄라 · 제미에듀 · 제주고 · 에듀인플랫폼(찾컨/랄랄라캠프)
- **8.8%**: 에듀인플랫폼(디지털새싹) · 제주대(찾컨) · 제주대(디싹) · 한국표준협회
- 처음 보는 업체는 `DEFAULT_TAX_RATE` = 3.3%
- **프리랜서는 3.3%로 두되 신뢰하지 말 것** — 발주처가 매번 달라 3.3%·8.8% 둘 다 실측됐다(2026-07-16 8.8% / 2026-09-02 3.3%).
- ⚠️ **소득구분은 가정 세율이 아니라 `(강의료 − 계좌 입금액)` 실측 세율로 판정한다** (±0.4%p 허용, 3.3%→사업소득 / 8.8%→기타소득 / 그 외 비움). 가정 세율로 판정하던 초기 구현은 2026-09-09 첫 정산에서 프리랜서 건을 '기타소득'으로 잘못 기록했다.
- ⚠️ 한국표준협회 2026-08-14 건은 노션에 세전 400,000 / 세액 60,000(=15%)로 남아 있으나, 실제로는 **항공비 등 출장비까지 합산한 금액에 8.8%** 를 적용한 것으로 확인됐다. 노션 행에 강의료만 적혀 세율이 왜곡돼 보인 사례 — 세율 판단 시 이 행을 근거로 쓰지 말 것.
- 출장비·교통비가 강의료와 함께 원천징수되는 경우: **강의료 칸에 합산 금액을 넣거나**, 계좌 입금액만 정확히 입력하면 차액이 세액으로 자동 기록된다.
- 세율 → 소득구분 자동 매핑: 3.3% → 사업소득, 8.8% → 기타소득, 그 외 비움

### 금액 배분 규칙
- 계좌 입금액과 페이백은 **강의료 비율로 건별 배분**하고, 반올림 끝수는 **마지막 건**에 몰아 합계를 정확히 맞춘다 (`alloc()`).
- 강의료가 모두 0이면 균등 배분한다.
- ⚠️ 노션 `주관업체`는 시트 업체명보다 세분화돼 있다(에듀인플랫폼(한라도서관)/(디지털새싹)/(찾컨)/(랄랄라캠프), 제주대(찾컨)/(디싹)). 정산 모달의 **드롭다운에서 직접 고른다** — `NOTION_CO_GUESS` 는 초기 선택값일 뿐이다.

### 정산 버튼 표시 조건 (셋 다 만족할 때만)
1. 강의 종료됨 (`isEventPast`)
2. 날짜 >= `SETTLE_START`(`2026-09-01`) — 그 이전 건은 이미 노션에 정리돼 있어 중복 방지
3. 업체가 `SALARY_COMPANIES`(대정중·남원중·백록초)가 **아님** — 월급제라 노션에 월 단위 행이 이미 있음

### 중복 방지 2중 장치
- 시트 `강의스케쥴` L열 `정산등록` = 'Y' 플래그
- 노션 `일정ID` 속성에 시트 ID 저장 → 이미 있으면 **새 행을 만들지 않고 그 행을 갱신**한다(`notionFindPage_` → PATCH)

### 노션 DB
`🎤 출강 및 근무 내역(예정 포함)` — `36b1717c-f78e-4efe-ac2e-2e1e404b4d5b`
연동용 속성: `일정ID`(텍스트) · `소득구분`(사업/기타/근로) · `페이백대상`(현민주/고인숙)

---

## 자격 · 연수 관리 (자격증현황 시트)

### 시트 스키마 (컬럼 8개)
```
A 카테고리 | B 명칭 | C 날짜 | D 발급/주관기관 | E 비고/결과 | F 급수/시간 | G 분류 | H ID
```
- **카테고리**: `자격증` 또는 `연수/교육`
- **F 급수/시간**: 자격증=급수(예 1급)·연수=이수시간(h)
- **G 분류**: 자격증 탭 `edtech`(에듀테크&SW·AI) / `national`(국가공인&전문자격). 비었으면 인덱스가 발급기관으로 자동 분류
- **H ID**: `readCertificates`가 비어 있으면 자동 부여(백필) — 수정/삭제에 필요

### 데이터 흐름
```
schedule.html 자격·연수 폼 → 자격증현황 시트 → (index 자격 섹션 카드 · 경력서 PDF) 자동 반영
```

---

## ⚠️ 배포(재배포) 주의
- `schedule-apps-script.txt`는 **schedule.html + resume-download.js 공용**. 자격·연수 액션 추가 등 스크립트 변경 시 **재배포 필요**.
- `apps-gallery-script.txt`는 **all.html 전용 별개 배포**. 앱 수정(update)·ID 컬럼 추가 등 변경 시 이 스크립트를 **따로 재배포**.
- 🚫 **앱갤러리 프로젝트는 재배포 금지** (위 '프로젝트 지도' 규칙 2 참조).
- 재배포는 **"배포 관리 → 기존 배포 편집 → 버전: 새 버전"**으로 하면 **URL이 유지**되어 코드 내 URL을 바꿀 필요가 없다. (새 배포를 만들면 URL이 바뀌어 해당 파일들의 URL을 모두 갱신해야 함)

---

## 개발 환경 주의 (Claude 작업용)
- 이 폴더는 **OneDrive 동기화** 폴더라, 셸(bash) 마운트가 **지연되어 옛/잘린 사본**을 읽을 수 있다. `node -c`가 파일 끝에서 "Unexpected end of input"을 내면 대개 동기화 지연이지 실제 오류가 아니다.
- **Read/Edit 도구가 authoritative**(사용자가 업로드할 실제 파일). 큰 JS/HTML 검증은 사본을 outputs 폴더(비-OneDrive)에 만들어 `node -c`로 확인.
